# AI 聊天助手技术规范

**版本**: 1.0.0
**最后更新**: 2026-01-22
**状态**: 需求定义完成

---

## 目录

- [0. 项目背景](#0-项目背景)
- [1. 核心定位与产品哲学](#1-核心定位与产品哲学)
- [2. 用户意图识别引擎](#2-用户意图识别引擎)
- [3. 澄清与交互策略](#3-澄清与交互策略)
- [4. 资源呈现与展示策略](#4-资源呈现与展示策略)
- [5. 响应策略与性能优化](#5-响应策略与性能优化)
- [6. 零结果处理：智能约束放松](#6-零结果处理智能约束放松)
- [7. 上下文与记忆策略](#7-上下文与记忆策略)
- [8. 移动端体验特化](#8-移动端体验特化)
- [9. 新用户引导：捷径诱惑策略](#9-新用户引导捷径诱惑策略)
- [10. 数据存储与隐私策略](#10-数据存储与隐私策略)
- [11. AI边界与成本优化](#11-ai边界与成本优化)
- [12. 技术实现规范](#12-技术实现规范)
- [13. UI/UX设计规范](#13-uiux设计规范)
- [14. 错误处理与降级策略](#14-错误处理与降级策略)
- [15. 测试策略](#15-测试策略)
- [16. 实施路线图](#16-实施路线图)
- [17. 成功指标（KPI）](#17-成功指标kpi)
- [18. 附录](#18-附录)

---

## 0. 项目背景

### 0.1 设计百宝箱 (Design Treasure Box)

**设计百宝箱**是一个精选设计资源聚合平台，为设计师和开发者提供高质量的设计美学参考与工具集。

#### 项目定位

设计百宝箱不仅是一个美学参考库，更是一个高效的工具集，旨在解决设计师和开发者在日常工作中的核心痛点：

- **灵感发现困难**：不知道从哪里找优质的设计参考
- **搜索效率低下**：传统搜索依赖精确关键词，无法理解自然语言描述
- **资源质量参差**：网络上的设计资源质量不均，筛选成本高
- **个性化需求强**：每个项目的风格、行业、受众各不相同，需要精准匹配

#### 核心功能

平台已实现的核心功能包括：

| 功能模块           | 描述                                                 | 技术实现                        |
| ------------------ | ---------------------------------------------------- | ------------------------------- |
| **资源分类浏览**   | 经过筛选的高质量设计资源，按类别、风格等维度分类展示 | 基于分类标签的静态展示          |
| **智能搜索与筛选** | 支持多维度（类别、颜色、风格等）的资源快速检索       | 传统关键词搜索 + 多条件筛选     |
| **专业评分系统**   | 深度展示资源的质量参考，包括综合评分和评分细分       | Supabase 数据库 + 评分计算逻辑  |
| **个人收藏系统**   | 本地持久化的用户收藏管理，支持跨会话保存             | LocalStorage + Zustand 状态管理 |
| **响应式设计**     | 完美适配桌面与移动端设备，提供一致的用户体验         | Tailwind CSS 响应式布局         |
| **深色/浅色主题**  | 原生支持系统级的主题切换，适应不同光线环境           | next-themes + CSS 变量          |
| **AI 集成**        | 内置 AI 能力辅助资源处理与交互（本文档重点）         | 智谱AI + Vercel AI SDK          |

#### 技术栈

项目采用前沿的现代化开发栈：

```typescript
技术架构：
├── 前端框架: Next.js 16 (App Router) + React 19
├── UI 组件库: shadcn/ui (Radix UI) + Tailwind CSS 4
├── 类型安全: TypeScript 5 (Strict Mode) + Zod Schema
├── 状态管理: Zustand + TanStack Query
├── 数据持久化: Supabase (Auth + DB) + LocalStorage
├── AI 能力: 智谱AI (GLM-4) + Vercel AI SDK
└── 动画库: Motion (Framer Motion)
```

---

### 0.2 AI聊天助手的角色与价值

#### 为什么需要AI聊天助手？

尽管设计百宝箱已经提供了分类浏览和多维度搜索功能，但我们在用户研究中发现了一些核心痛点：

**痛点1：自然语言表达 vs 精确关键词**

```
用户想法： "我想找一些那种蓝白配色、看起来很专业、适合B2B SaaS公司的官网设计"
传统搜索： 需要拆解为 "蓝色 + 白色 + SaaS + 官网"，但"专业"这个主观感受无法搜索
AI聊天：   理解自然语言描述，智能提取核心维度（颜色、行业、风格、受众），自动推荐匹配资源
```

**痛点2：灵感探索 vs 目标明确**

```
目标明确： "我要找电商网站的结账页面案例" → 传统搜索高效
灵感探索： "我想看看最近流行什么样的设计风格" → 传统搜索无力
AI聊天：   根据意图动态调整策略，提供探索式推荐和灵感引导
```

**痛点3：搜索结果 vs 推荐理由**

```
传统搜索： 返回100个结果，用户逐个点击查看，耗时耗力
AI聊天：   每个推荐都附带"推荐理由"，解释"为什么匹配"，帮助用户快速决策
```

**痛点4：静态筛选 vs 迭代优化**

```
传统搜索： 用户手动调整筛选条件，需要重新理解每个筛选器的含义
AI聊天：   对话式迭代，用户说"再推荐一些类似的"或"不要太极简"，AI自动理解并优化
```

#### AI聊天助手的核心价值

| 价值维度     | 传统方式               | AI聊天助手            | 提升效果            |
| ------------ | ---------------------- | --------------------- | ------------------- |
| **搜索门槛** | 需要知道专业术语和分类 | 自然语言描述需求      | 降低 70% 的使用门槛 |
| **搜索效率** | 多次尝试关键词组合     | 一次提问 + 澄清优化   | 减少 50% 的搜索时间 |
| **结果质量** | 依赖用户筛选能力       | AI理解意图 + 推荐理由 | 提升 80% 的满意度   |
| **灵感发现** | 需要主动浏览多个分类   | AI主动推荐相关资源    | 增加 3 倍资源发现量 |

#### 与现有功能的关系

AI聊天助手不是替代现有功能，而是**增强和补充**：

```
设计百宝箱功能矩阵：

┌─────────────────────────────────────────────────────┐
│                                                     │
│  【发现层】                                          │
│  - 分类浏览：用户主动探索                           │
│  - AI聊天：AI主动推荐 + 对话式探索 ← 新增           │
│                                                     │
│  【搜索层】                                          │
│  - 传统搜索：精确关键词匹配                         │
│  - AI聊天：自然语言理解 + 语义搜索 ← 增强           │
│                                                     │
│  【交互层】                                          │
│  - 筛选器：手动调整条件                             │
│  - AI聊天：对话式迭代优化 ← 增强                    │
│                                                     │
│  【展示层】                                          │
│  - 资源卡片：基础信息展示                           │
│  - AI聊天：推荐理由 + 匹配度分析 ← 增强             │
│                                                     │
└─────────────────────────────────────────────────────┘
```

**互补策略**：

- **精确搜索场景**：用户有明确的筛选条件时，传统搜索更快更直接
- **模糊探索场景**：用户需求不清晰或需要灵感时，AI聊天更有优势
- **混合使用场景**：用户可以先通过AI聊天找到方向，再用传统搜索精准筛选

---

### 0.3 目标用户与使用场景

#### 核心用户群体

**用户画像1：UI/UX设计师**

- **特征**：需要大量设计灵感，关注设计趋势和风格
- **需求**：快速找到符合项目风格和行业的参考案例
- **AI价值**：自然语言描述设计需求，获得精准推荐

**用户画像2：前端开发者**

- **特征**：需要实现具体的设计效果，关注技术实现
- **需求**：找到可参考的代码实现和设计模式
- **AI价值**：描述功能需求，找到符合技术栈的设计参考

**用户画像3：产品经理**

- **特征**：需要竞品分析和行业参考，关注用户体验
- **需求**：了解同行业的设计趋势和最佳实践
- **AI价值**：描述业务场景，找到行业标杆案例

**用户画像4：设计学生/初学者**

- **特征**：设计经验有限，不知道如何描述需求
- **需求**：学习和模仿优秀设计，建立设计语感
- **AI价值**：通过引导式提问和推荐理由，学习设计知识

#### 典型使用场景

**场景1：项目初期灵感收集**

```
用户： "我正在做一个医疗健康类的SaaS产品，想找一些既能体现专业性，
       又不会给人冷冰冰感觉的官网设计案例。"

AI：  [识别到：医疗健康 + SaaS + 专业但不冰冷]
     → 推荐"温暖色调的医疗科技网站"
     → 解释每个推荐为什么符合"专业但不冰冷"
     → 提供风格变体建议（"如果您希望更亲和一些，可以看看这些..."）
```

**场景2：快速竞品分析**

```
用户： "帮我找一些电商网站的结账页面设计，我要看看怎么减少用户流失。"

AI：  [识别到：电商 + 结账页面 + 优化目标]
     → 推荐"高转化率的电商结账页面"
     → 重点标注"减少流失"的关键设计点
     → 对比不同设计模式（"单页结账 vs 分步结账"）
```

**场景3：设计风格探索**

```
用户： "最近好像很流行那种3D元素的设计，给我看看有哪些案例？"

AI：  [识别到：模糊探索意图 + 3D趋势]
     → 推荐"3D元素设计案例集合"
     → 按行业分组（科技、艺术、电商）
     → 提供"如果您喜欢3D，可能也会喜欢..."的相关风格推荐
```

**场景4：快速解决设计问题**

```
用户： "我要在一个深色背景上展示大量数据，应该怎么设计才不会显得混乱？"

AI：  [识别到：深色背景 + 数据可视化 + 信息清晰度]
     → 推荐"深色主题的数据大屏设计"
     → 重点标注"信息层次"和"视觉焦点"的处理方式
     → 提供设计原则建议（"使用渐进透明度..."）
```

---

### 0.4 项目愿景与AI聊天助手的未来

#### 短期目标（3-6个月）

**完善核心体验**：

- ✅ 实现智能意图识别和推荐引擎
- ✅ 优化零结果处理和迭代优化
- ✅ 提升移动端体验
- ✅ 完善用户引导和新手体验

**预期成果**：

- AI聊天助手的日均使用率达到 30%
- 用户满意度评分 ≥ 4.5/5
- 平均每次对话找到3+个有用资源

#### 中期目标（6-12个月）

**增强AI能力**：

- 多模态搜索：支持上传图片找相似设计
- 个性化推荐：基于用户历史行为优化推荐
- 协作功能：支持分享对话和推荐结果
- 教学模式：AI主动讲解设计知识和原理

**预期成果**：

- 成为用户首选的资源发现方式
- 用户留存率提升 50%
- 社区分享率 ≥ 20%

#### 长期愿景（1-2年）

**构建设计知识生态**：

- AI设计顾问：从"找资源"扩展到"解决设计问题"
- 社区知识图谱：基于用户行为构建设计知识网络
- 趋势预测：AI分析设计趋势并提前预警
- 创作辅助：AI辅助生成设计方案和原型

**最终目标**：
成为设计师和开发者的**智能设计伙伴**，而不仅仅是资源搜索工具。

---

## 1. 核心定位与产品哲学

### 1.1 核心价值定位

AI聊天助手采用**混合模式**，根据用户意图动态调整策略：

- **精确搜索模式**：当用户意图清晰时，提供精准的资源推荐
- **灵感探索模式**：当用户需求模糊时，引导发现和探索设计资源
- **渐进式澄清**：在搜索和探索间平滑过渡，通过自适应策略逐步明确用户需求

### 1.2 设计哲学

> **不要用教程教育用户，要用"捷径"诱惑用户。先让用户尝到甜头，再教他们如何更专业地提问。**

关键原则：

- **智能降级**：绝不空手而归，零结果时提供近似推荐
- **量质双重验证**：避免单一指标缺陷，结合关键词密度和维度完整性
- **透明化AI决策**：通过推荐理由让用户理解"为什么推荐这个"

---

## 2. 用户意图识别引擎

### 2.1 混合判定逻辑（量 + 质）

采用**漏斗式判定流程**，结合两个维度：

**维度1：关键词密度（看"量"）**

```
除去停用词（如"的"、"网站"），有效关键词是否 < 2 个？
- 🔴 低密度：< 2个有效关键词
- 🟡 中密度：2-3个有效关键词
- 🟢 高密度：≥ 4个有效关键词
```

**维度2：核心维度完整性（看"质"）**

```
核心维度包括：
- [类别/行业]：电商、SaaS、博客、医疗等
- [风格]：极简、复古、赛博朋克、蒸汽朋克等
- [颜色]：蓝色、红色、渐变等
- [受众]：B2B、B2C、年轻、专业等
```

### 2.2 判定矩阵

| 用户输入         | 关键词密度 | 核心维度完整性 | 最终判定与策略                  |
| ---------------- | ---------- | -------------- | ------------------------------- |
| "蓝色"           | 🔴 低      | ❌ 缺类别      | 强引导（批量澄清：行业+风格）   |
| "蓝色 简约"      | 🟡 中      | ❌ 缺类别      | 弱引导（单次澄清：行业？）      |
| "蓝色 电商"      | 🟡 中      | ✅ 有类别      | 直接搜 + 非侵入建议（补充风格） |
| "蓝色 简约 电商" | 🟢 高      | ✅ 有类别      | 精准搜（直接展示最佳匹配）      |

### 2.3 场景推演示例

**场景 A：极度模糊**

```
用户输入: "蓝色" 或 "蓝色网站"
第一层（密度）: 有效词仅 1 个 → 低密度
第二层（维度）: 缺失 [类别]、[风格] → 低完整性
→ 结论: 极度模糊
→ 系统反应: 批量多维度澄清
```

**场景 B：信息丰富但方向不明**

```
用户输入: "好看的蓝色网站"
第一层（密度）: 有效词 2 个（好看、蓝色）→ 中密度
第二层（维度）: "好看"是主观修饰词，缺失核心维度 [类别]
→ 结论: 信息丰富但方向不明
→ 系统反应: 单次渐进澄清 或 非侵入式建议
文案: "您是想找特定行业的案例吗？"
```

**场景 C：意图清晰**

```
用户输入: "蓝色 电商 网站"
第一层（密度）: 有效词 2 个 → 中密度
第二层（维度）: 包含核心维度 [类别：电商]
→ 结论: 意图清晰
→ 系统反应: 直接展示结果 + 侧边栏筛选
```

---

## 3. 澄清与交互策略

### 3.1 自适应澄清策略

根据查询清晰度动态选择澄清方式：

**高模糊度（密度低+维度缺失）→ 批量多维度澄清**

- 一次识别所有模糊维度
- 同时展示2-3个澄清问题（风格？类别？受众？）
- 示例：
  ```
  想帮您找到更合适的资源，请问：
  1. 您是哪个行业？（电商/SaaS/博客/医疗...）
  2. 偏好什么风格？（极简/复古/3D/赛博朋克...）
  ```

**中等模糊度（密度中+部分维度）→ 单次渐进澄清**

- 每次只问一个问题
- 根据用户回答动态调整下一个问题
- 优点：更自然、更个性化

**低模糊度（密度高+核心维度完整）→ 非侵入式建议**

- 先返回搜索结果
- 在结果下方展示"您是否在找..."的澄清建议
- 用户可以忽略也可以点击

### 3.2 澄清问题的UI呈现

**批量澄清UI**：

- 使用卡片式布局，每个澄清问题一个卡片
- 提供2-4个预设选项按钮
- 底部提供"自定义输入"选项

**单次澄清UI**：

- 聊天气泡形式
- 选项作为快速回复按钮
- 保持对话流畅性

**非侵入式建议UI**：

- 在搜索结果底部/侧边栏
- 使用轻度视觉样式（灰色、小字号）
- 明确标识为"建议"而非"问题"

---

## 4. 资源呈现与展示策略

### 4.1 宏观布局：泳道式分组展示

**按推荐策略分组**：

```
基于您的搜索，我们将结果分为：
┌─────────────────────────────────────┐
│ 【科技蓝】B2B SaaS & 科技公司案例    │
│ [← 滑动查看更多 →]                  │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ 【医疗蓝】医疗健康行业精选           │
│ [← 滑动查看更多 →]                  │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ 【创意蓝】设计机构 & 作品集案例       │
│ [← 滑动查看更多 →]                  │
└─────────────────────────────────────┘
```

**分组维度选择**（动态调整）：

- 当用户搜索"蓝色"时 → 按行业分组（科技蓝、医疗蓝、创意蓝）
- 当用户搜索"电商"时 → 按风格分组（极简、复古、3D）
- 每组3-5个资源，避免信息过载

### 4.2 微观卡片：AI推荐语

**卡片结构**：

```
┌─────────────────────────────────────┐
│ [资源缩略图]                         │
│                                     │
│ Stripe 官网                          │
│ ⭐⭐⭐⭐⭐ 4.9  |  💾  ⚡  🔗      │
│                                     │
│ 💡 AI 推荐语                         │
│ "匹配度 92%：完美的 B2B 落地页       │
│  结构，渐变紫色的数据可视化部分      │
│  非常适合您的需求。"                 │
└─────────────────────────────────────┘
```

**推荐语原则**：

- **说人话**：不要只列参数，要解释为什么匹配
- **强调亮点**：指出具体匹配的方面（颜色、风格、布局）
- **提供匹配度**：给出相似度分数（如92%）
- **关联查询**：明确提到与用户查询的关联点

### 4.3 动态迭代与优化

**每组末尾提供迭代入口**：

```
┌─────────────────────────────────────┐
│ 不喜欢这组？                         │
│ [🔄 告诉我调整方向]                  │
│                                     │
│ 💬 输入框                             │
│ "更极简一点" | "换暖色调" | ...      │
└─────────────────────────────────────┘
```

**支持的迭代指令**：

- "再推荐一些类似的" → 基于当前结果扩展搜索
- "不要太极简的" → 排除极简风格，重新推荐
- "更现代一点" → 调整风格向现代方向
- "换颜色" → 保持其他条件，调整颜色偏好

### 4.4 基础交互操作

每个资源卡片提供：

- 💾 **收藏**：快速收藏到个人收藏夹
- ⚡ **预览**：快速预览资源（不跳转）
- 🔗 **访问**：跳转到资源详情页/外链
- 📤 **分享**：分享资源链接

**批量操作**（可选）：

- 长按卡片进入批量选择模式
- 支持批量收藏、批量对比

---

## 5. 响应策略与性能优化

### 5.1 三阶段渐进式加载

采用**带有骨架屏的渐进式加载**策略，操控用户感知等待时间：

**阶段一：即时反馈与骨架屏 (0s - 0.5s)**

```
用户看到：
┌─────────────────────────────────────┐
│ ✨ 正在分析234个资源...              │
│                                     │
│ ┌──────┐ ┌──────┐ ┌──────┐        │
│ │ 🦴   │ │ 🦴   │ │ 🦴   │  骨架屏 │
│ │ Card │ │ Card │ │ Card │  (4-6个) │
│ └──────┘ └──────┘ └──────┘        │
└─────────────────────────────────────┘
```

**目的**：

- 抢占视线：告诉用户"东西马上来"
- 稳定布局：撑开页面高度，避免累积布局偏移 (CLS)

**阶段二：首屏高置信度填充 (0.5s - 1.5s)**

```
优先分析完 Top 1-3 结果：
┌─────────────────────────────────────┐
│ ✨ 正在分析234个资源...              │
│                                     │
│ ┌──────┐ ┌──────┐ ┌──────┐        │
│ │ ✅   │ │ ✅   │ │ 🦴   │        │
│ │真实  │ │真实  │ │骨架  │        │
│ │内容  │ │内容  │ │      │        │
│ └──────┘ └──────┘ └──────┘        │
└─────────────────────────────────────┘
```

**目的**：

- 满足核心需求：用户通常只看前几个
- 当用户阅读 Top 1 时，后台继续处理剩下的

**阶段三：懒加载剩余资源 (1.5s+)**

```
悄悄替换剩余骨架卡片：
┌─────────────────────────────────────┐
│ ✨ 找到 12 个匹配资源                │
│                                     │
│ ┌──────┐ ┌──────┐ ┌──────┐        │
│ │ ✅   │ │ ✅   │ │ ✅   │        │
│ │真实  │ │真实  │ │真实  │        │
│ │内容  │ │内容  │ │内容  │        │
│ └──────┘ └──────┘ └──────┘        │
│                                     │
│ [加载更多...]                        │
└─────────────────────────────────────┘
```

**目的**：

- 降低首屏计算压力
- 平衡速度与数量

### 5.2 流式输出策略

**文本流式 + 资源完整加载**：

- AI回答文本逐字流式输出
- 资源推荐等待完整分析后一次性展示
- 优点：用户感觉响应快，资源卡片不会闪烁

**为什么不采用全流式？**

- **累积布局偏移 (CLS)**：卡片动态加载会导致页面跳动
- **用户体验差**：用户刚想点击，新卡片挤出来，点错

### 5.3 性能优化关键指标

**响应时间目标**：

- 简单查询（纯向量检索）：< 1s
- 标准查询（RAG + 轻量生成）：1-2s
- 复杂查询（深度分析）：3-5s

**用户体验目标**：

- 首次内容绘制 (FCP)：< 0.5s
- 最大内容绘制 (LCP)：< 1.5s
- 累积布局偏移 (CLS)：< 0.1

---

## 6. 零结果处理：智能约束放松

### 6.1 核心逻辑

当 `A + B + C = 0 结果`时，系统不应直接放弃，而应该尝试计算 `A+B`、`B+C`、`A+C` 的结果。

### 6.2 三层降级策略

**第一层：诚实告知 + 维度拆解推荐**

示例：用户搜索"紫色蒸汽朋克风格的宠物医院网站"

```
┌─────────────────────────────────────┐
│ 暂无完全匹配的"紫色蒸汽朋克宠物医院"  │
│ 这个组合太独特了！但 AI 猜您可能     │
│ 对以下方向感兴趣：                   │
└─────────────────────────────────────┘

【选项卡 A：看风格】
蒸汽朋克灵感 (12个)
推荐语：虽然不是宠物医院，但这些游戏官网的
紫色蒸汽朋克视觉非常惊艳。

【选项卡 B：看行业】
宠物医院精选 (8个)
推荐语：这是目前评分最高的宠物医院设计，
主要为温馨/医疗风格。
```

**第二层：引导式调整**

提供可删除的搜索条件标签：

```
┌─────────────────────────────────────┐
│ 您正在搜索：                         │
│ [ x 紫色 ] [ x 蒸汽朋克 ] [ x 宠物医院 ] │
│                                     │
│ 点击 'x' 删掉"蒸汽朋克"，             │
│ 结果立刻刷新为普通的紫色宠物医院      │
└─────────────────────────────────────┘
```

**第三层：语义关联拓展**

如果连拆解后的结果也很少，AI联想相关概念：

```
┌─────────────────────────────────────┐
│ 也许您可以参考一下：                 │
│                                     │
│ 💡 "蒸汽朋克" ≈ "复古机械"           │
│ 💡 "蒸汽朋克" ≈ "工业风"             │
│                                     │
│ [探索工业风案例] [探索复古机械案例]   │
└─────────────────────────────────────┘
```

### 6.3 关键原则

1. **不要让用户空手而归**：哪怕结果不对，也要给点相关的
2. **解释为什么给这些**：区分是"匹配了风格"还是"匹配了行业"
3. **把修改权交给用户**：用标签(Chips)让用户做减法

---

## 7. 上下文与记忆策略

### 7.1 短期会话记忆

**策略**：

- **会话内记忆**：记住用户的澄清选择、反馈（"不要太极简"）、迭代方向
- **会话结束后清空**：关闭聊天窗口即清空记忆
- **不持久化到服务器**：只在浏览器localStorage中保存

**记忆内容**：

```typescript
interface SessionMemory {
  sessionId: string;
  preferences: {
    categories?: string[]; // 用户选择的类别
    styles?: string[]; // 用户选择的风格
    excludedStyles?: string[]; // 用户排除的样式
    colorPreferences?: string[]; // 用户偏好的颜色
  };
  feedbackHistory: {
    query: string;
    feedback: string; // "更极简一点" | "不要太极简"
    timestamp: Date;
  }[];
  lastUpdated: Date;
}
```

**记忆的应用场景**：

- 用户说"再推荐一些类似的" → 基于当前结果和偏好扩展
- 用户说"不要太极简的" → 在会话内记住，后续推荐排除极简风
- 用户澄清了"我要B2B的" → 后续推荐优先B2B资源

### 7.2 为什么不使用长期记忆？

**理由**：

1. **隐私友好**：用户无隐私顾虑
2. **实现简单**：不需要复杂的用户系统
3. **避免过拟合**：长期偏好可能导致推荐窄化

**未来扩展方向**：

- 提供"保存会话"功能（可选云同步）
- 允许用户手动标记"这是我的持久偏好"

---

## 8. 移动端体验特化

### 8.1 布局策略

**全屏模式**（移动端）：

- AI聊天界面占满整个屏幕
- 确保有足够空间展示资源卡片
- 手势返回关闭聊天

**抽屉模式**（桌面端）：

- 右侧固定宽度抽屉（340px - 400px）
- 半透明遮罩层
- 点击遮罩关闭聊天

### 8.2 手势优化

**卡片交互**：

- 支持左右滑动查看更多资源
- 长按进入批量选择模式
- 上滑快速滚动到下一组

**导航手势**：

- 左右滑动切换分组（科技蓝 / 医疗蓝 / 创意蓝）
- 下拉刷新推荐结果
- 侧滑返回关闭聊天

### 8.3 响应式适配

**布局调整**：

```
桌面端：[横向泳道]
┌───────────────────────────────┐
│ 【科技蓝】← 滑动查看更多 →    │
│ [Card][Card][Card][Card]      │
└───────────────────────────────┘

移动端：[纵向泳道]
┌───────────────────────────────┐
│ 【科技蓝】                    │
│ ┌──────┐                      │
│ │Card  │                      │
│ └──────┘                      │
│ ┌──────┐                      │
│ │Card  │                      │
│ └──────┘                      │
│ [查看更多 12 个资源 ↓]         │
└───────────────────────────────┘
```

**触摸优化**：

- 按钮最小尺寸：44x44px (Apple HIG标准)
- 卡片间距增大，便于点击
- 文字和图标适配小屏幕

---

## 9. 新用户引导：捷径诱惑策略

### 9.1 第一步：人性化开场

```
┌─────────────────────────────────────┐
│           [AI Logo/形象]             │
│                                     │
│ 嗨！设计灵感枯竭了？                 │
│ 告诉我你想找什么，                   │
│ 或者试试下面这些方向 👇              │
└─────────────────────────────────────┘
```

### 9.2 第二步：一键提问（胶囊按钮）

提供 3-4 个不同维度的完整 Prompt，点击即发送：

```
┌─────────────────────────────────────┐
│ ┌─────────────────────────────────┐ │
│ │ ✨ 看看最近热门的网页设计        │ │
│ └─────────────────────────────────┘ │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ 🎨 帮我找一些赛博朋克风的配色    │ │
│ └─────────────────────────────────┘ │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ 🏢 适合医疗行业的B2B官网案例     │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

**设计要点**：

- 覆盖新手到高手的不同场景
- 卡片内容可以随机轮换，保持新鲜感
- 使用精美的图标/emoji吸引点击

### 9.3 第三步：填空式引导（Mad Libs Style）

教用户"如何提问"：

```
┌─────────────────────────────────────┐
│ 不知道怎么描述？试试填空：           │
│                                     │
│ "我想找一个                         │
│  [ 🎨 风格 ] 的                      │
│  [ 🏢 行业 ] 网站，                  │
│  主色调是 [ 🎨 颜色 ]。"             │
│                                     │
│ 点击任意标签选择选项 👆              │
└─────────────────────────────────────┘
```

**交互**：

- 点击 [风格] → 弹出选项（极简、复古、3D...）
- 点击 [行业] → 弹出选项（电商、SaaS、博客...）
- 点击 [颜色] → 弹出选项（蓝色、红色、渐变...）

**教育价值**：
潜移默化地告诉用户：AI听得懂风格、行业和颜色这三个维度的组合。

### 9.4 输入框增强

**Placeholder 动效**：

```
输入框内自动打字演示：
"输入'红色的科技网站'..."
→ 自动删除
→ "输入'极简风电商'..."
→ 自动删除
→ 循环
```

**预设问题快捷按钮**（输入框下方）：

```
[推荐热门资源] [按颜色浏览] [按行业浏览] [B2B案例]
```

---

## 10. 数据存储与隐私策略

### 10.1 本地会话存储（现有实现）

**策略**：

- 只在浏览器 `localStorage` 中保存当前会话
- 不发送到服务器
- 关闭聊天窗口或清缓存时丢失历史

**存储结构**：

```typescript
interface LocalSession {
  sessionId: string;
  messages: ExtendedChatMessage[];
  maxMessages: 50; // 最多保存50条消息
  createdAt: Date;
  updatedAt: Date;
}
```

**存储Key**：

```typescript
const STORAGE_KEY = 'ai-chat-session';
```

### 10.2 为什么选择本地存储？

**优点**：

- ✅ 隐私友好：用户数据不离开浏览器
- ✅ 实现简单：无需复杂的后端存储
- ✅ 成本低：无服务器存储成本
- ✅ 性能好：本地读写速度快

**缺点**：

- ❌ 换设备丢失历史
- ❌ 清缓存丢失数据
- ❌ 无法跨设备同步

**权衡**：对于设计资源推荐工具，隐私和简单性 > 跨设备同步。

### 10.3 未来扩展方向

**可选云同步**：

```typescript
interface CloudSyncConfig {
  enabled: boolean; // 用户显式开启
  requiresAuth: boolean;
  syncFrequency: 'realtime' | 'manual' | 'onClose';
  dataRetention: '7days' | '30days' | 'forever';
}
```

**功能**：

- 提供"保存到云端"的显式开关（需登录）
- 用户完全控制数据是否上传
- 适合有多设备需求的用户

---

## 11. AI边界与成本优化

### 11.1 混合策略：默认L2 + 短路逻辑L1

**L1：纯向量检索（快速响应）**

```
流程：
用户查询 → 向量化 → 向量搜索 → 返回Top结果

特点：
- 响应时间：< 1s
- AI成本：低（仅embedding）
- 用户体验：快速但无解释
```

**L2：检索 + 轻量生成（智能增强）**

```
流程：
用户查询 → 向量化 → 向量搜索 → AI生成推荐理由 → 返回结果

特点：
- 响应时间：1-2s
- AI成本：中（embedding + 轻量generation）
- 用户体验：有推荐理由和澄清问题
```

**L3：深度AI分析（智能最大化）**

```
流程：
用户查询 → 向量搜索 → AI深度分析 → 每条详细理由 → 多维澄清 → 返回

特点：
- 响应时间：3-5s
- AI成本：高（大量token）
- 用户体验：最佳但慢
```

### 11.2 短路逻辑（成本优化）

**起步阶段策略**：

```
默认使用 L2（标准RAG）

加一个"短路逻辑"：
IF 查询词 < 5个字 AND 包含[颜色/行业]关键词:
    切换到 L1 模式（纯向量检索）
ELSE:
    使用 L2 模式（标准RAG）
```

**示例**：

```
"蓝色" → L1（纯向量检索，快速返回）
"蓝色 电商" → L2（标准RAG，生成推荐理由）
"适合B2B SaaS且能体现数据可视化的极简风格" → L2（标准RAG）
```

### 11.3 成本控制

**目标**（每次查询）：

- L1模式：< ¥0.01
- L2模式：¥0.02 - ¥0.05
- L3模式：¥0.10 - ¥0.20（谨慎使用）

**优化措施**：

1. **缓存常见查询**：相同查询24小时内直接返回缓存结果
2. **批量embedding**：预生成所有资源的embedding，避免实时计算
3. **智能降级**：复杂查询先尝试L2，用户不满意再升级L3
4. **Token控制**：限制AI生成的内容长度（推荐理由≤50字）

---

## 12. 技术实现规范

### 12.1 前端架构

**组件结构**：

```
components/
├── ai-chat-interface.tsx          # 主聊天界面
├── ai-chat/
│   ├── resource-message.tsx       # 资源推荐消息
│   ├── clarification-message.tsx  # 澄清问题消息
│   ├── skeleton-card.tsx          # 骨架屏卡片
│   ├── resource-card.tsx          # 资源卡片
│   └── chat-input.tsx             # 聊天输入框
└── prompt-kit/                     # 可复用的聊天组件
    ├── chat-container.tsx
    ├── message.tsx
    ├── prompt-input.tsx
    └── loader.tsx
```

**状态管理**：

```typescript
// hooks/use-ai-chat.ts
export function useAIChat() {
  const [isOpen, setIsOpen] = useState(false);
  const [messages, setMessages] = useState<ExtendedChatMessage[]>([]);
  const [sessionId, setSessionId] = useState<string>('default');
  const [isLoading, setIsLoading] = useState(false);

  // LocalStorage同步
  // 会话管理
  // 消息增删改查
}
```

### 12.2 API设计

**POST /api/chat**

```typescript
interface ChatRequest {
  query: string;
  filters: {
    maxResults: number;
    categories?: string[];
    styles?: string[];
    minRating?: number;
  };
  conversationHistory: {
    type: 'user' | 'assistant';
    content: string;
  }[];
  sessionId: string;
}

interface ChatResponse {
  success: boolean;
  data: {
    content: string; // AI的回答文本
    searchResults: {
      resource: Resource;
      similarity: number;
      matchReason: string;
    }[];
    clarificationQuestions?: {
      question: string;
      options: string[];
      aspect: 'category' | 'style' | 'audience' | 'purpose';
    }[];
    searchMetadata: {
      query: string;
      filters: SearchFilters;
      resultCount: number;
      processingTime: number;
      searchType: 'semantic' | 'hybrid' | 'fallback';
    };
  };
  error?: string;
}
```

### 12.3 后端逻辑

**意图识别引擎**：

```typescript
// lib/ai/query-analyzer.ts
export class QueryAnalyzer {
  analyzeQuery(query: string): QueryAnalysis {
    // 1. 计算关键词密度
    const density = this.calculateKeywordDensity(query);

    // 2. 检查核心维度完整性
    const dimensions = this.checkDimensions(query);

    // 3. 判定清晰度
    const clarity = this.determineClarity(density, dimensions);

    return {
      clarity,
      density,
      missingDimensions: dimensions.missing,
      presentDimensions: dimensions.present,
      confidence: this.calculateConfidence(clarity, density),
    };
  }
}
```

**推荐引擎**：

```typescript
// lib/ai/recommendation-engine.ts
export class RecommendationEngine {
  async recommend(
    query: string,
    analysis: QueryAnalysis,
    filters: SearchFilters
  ): Promise<SearchResult[]> {
    // 1. 向量化查询
    const embedding = await this.embedQuery(query);

    // 2. 向量搜索
    let results = await this.vectorSearch(embedding, filters);

    // 3. 智能约束放松（如果结果少）
    if (results.length < 3) {
      results = await this.constraintRelaxation(query, filters);
    }

    // 4. 生成推荐理由（L2模式）
    if (this.shouldUseL2(analysis)) {
      results = await this.generateRecommendationReasons(results, query);
    }

    return results;
  }
}
```

### 12.4 AI提供商配置

**环境变量**：

```env
# 智谱AI配置
ZHIPU_AI_API_KEY=your_api_key
ZHIPU_AI_MODEL=glm-4-plus  # 推荐：增强版
ZHIPU_AI_BASE_URL=https://open.bigmodel.cn/api/paas/v4/

# Embedding模型
ZHIPU_AI_EMBEDDING_MODEL=embedding-3

# 流式响应开关
ENABLE_STREAMING=true

# 其他提供商（备用）
OPENAI_API_KEY=your_openai_key
ANTHROPIC_API_KEY=your_anthropic_key
```

**模型选择**：

```typescript
// 推荐配置
const AI_CONFIG: ZhipuAIConfig = {
  provider: 'zhipu-ai',
  model: 'glm-4-plus', // 文本生成：增强版，性能最好
  embeddingModel: 'embedding-3', // 向量化
  streamingEnabled: true,
  maxTokens: 1000, // 控制成本
  temperature: 0.7, // 平衡创造性和准确性
};
```

---

## 13. UI/UX设计规范

### 13.1 视觉风格

**整体风格**：

- 现代简约，符合设计工具的定位
- 柔和的圆角（16px - 24px）
- 微妙的阴影和模糊效果
- 动画流畅自然（spring动画）

**颜色系统**：

```
Primary: 蓝色系（科技感）
Secondary: 灰色系（中性）
Accent: 绿色（在线状态、成功）
Error: 红色（错误提示）
Warning: 橙色（警告）
```

### 13.2 动画规范

**过渡动画**：

```
淡入淡出: 200ms - 300ms
滑动: 300ms - 400ms (spring)
缩放: 200ms (ease-out)
```

**加载动画**：

- 骨架屏：轻微脉冲动画
- 消息加载：三点跳动的 DotsLoader
- 流式输出：打字机效果

**微交互**：

- 按钮hover: 轻微放大 + 阴影增强
- 卡片hover: 上浮 + 阴影增强
- 收藏按钮: 心跳动画

### 13.3 响应式断点

```
Mobile: < 640px
Tablet: 640px - 1024px
Desktop: > 1024px
Large Desktop: > 1280px
```

**布局适配**：

- Mobile: 全屏聊天，纵向泳道
- Tablet: 抽屉宽度340px，纵向泳道
- Desktop: 抽屉宽度380px - 400px，横向泳道

### 13.4 可访问性

**键盘导航**：

- Tab: 焦点移动
- Enter: 发送消息 / 选择选项
- Escape: 关闭聊天
- Arrow Keys: 在选项间导航

**屏幕阅读器**：

- 所有按钮有明确的 aria-label
- 消息有正确的角色标识 (role="log")
- 加载状态有 aria-live

**颜色对比度**：

- 文本对比度 ≥ 4.5:1 (WCAG AA)
- 大文本对比度 ≥ 3:1 (WCAG AA)

---

## 14. 错误处理与降级策略

### 14.1 错误类型

**API错误**：

```typescript
interface APIError {
  type: 'timeout' | 'rate_limit' | 'invalid_key' | 'server_error';
  message: string;
  retryable: boolean;
}
```

**处理策略**：

```
Timeout (请求超时):
→ 显示："请求超时，请重试"
→ 提供"重试"按钮

Rate Limit (速率限制):
→ 显示："请求过于频繁，请稍后再试"
→ 显示"冷却倒计时"

Invalid Key (API密钥无效):
→ 显示："服务配置错误，请联系管理员"
→ 不显示重试按钮

Server Error (服务器错误):
→ 显示："服务暂时不可用，请稍后重试"
→ 提供重试按钮
```

### 14.2 降级策略

**AI服务降级**：

```
L3 (深度分析) 失败 → 降级到 L2
L2 (标准RAG) 失败 → 降级到 L1
L1 (向量检索) 失败 → 显示错误 + 反馈入口
```

**前端降级**：

```
骨架屏加载失败 → 显示占位符
图片加载失败 → 显示默认图标
流式输出失败 → 等待完整响应
```

### 14.3 用户反馈

**反馈入口**：

```
┌─────────────────────────────────────┐
│ 😔 没找到您想要的？                  │
│                                     │
│ 告诉我们哪里出了问题，帮助我们改进。 │
│                                     │
│ [📝 提交反馈]  [🔄 重新搜索]        │
└─────────────────────────────────────┘
```

**反馈内容**：

- 用户查询
- 返回结果数量
- 用户是否满意
- 具体问题描述

---

## 15. 测试策略

### 15.1 功能测试

**核心功能**：

- [ ] 用户发送消息 → AI返回结果
- [ ] 澄清问题 → 用户选择 → 结果更新
- [ ] 点击资源卡片 → 跳转详情页
- [ ] 收藏资源 → 成功提示
- [ ] 迭代优化 → 新结果返回

**边界情况**：

- [ ] 零结果 → 智能约束放松
- [ ] 超长查询 → 截断或分词
- [ ] 特殊字符 → 正确处理
- [ ] 空输入 → 不发送或提示

### 15.2 性能测试

**响应时间**：

- [ ] L1查询 < 1s
- [ ] L2查询 < 2s
- [ ] 首屏渲染 < 1.5s
- [ ] 骨架屏显示 < 0.5s

**并发测试**：

- [ ] 10个并发用户 → 无显著延迟
- [ ] 100个并发请求 → 优雅降级或队列

### 15.3 用户体验测试

**A/B测试**：

- [ ] 引导方式：一键提问 vs 纯文字引导
- [ ] 骨架屏：有骨架屏 vs 无骨架屏
- [ ] 澄清策略：批量 vs 单次

**用户满意度**：

- [ ] 结果相关性评分
- [ ] 推荐理由帮助度
- [ ] 整体满意度 (NPS)

### 15.4 安全测试

**输入验证**：

- [ ] XSS攻击过滤
- [ ] SQL注入防护
- [ ] Prompt注入防护

**数据隐私**：

- [ ] 不记录敏感信息
- [ ] LocalStorage数据加密
- [ ] API密钥安全存储

---

## 16. 实施路线图

### Phase 1: 核心功能（2-3周）

**Week 1: 基础架构**

- [ ] 搭建前后端通信框架
- [ ] 实现意图识别引擎（量+质双重验证）
- [ ] 实现向量检索 + RAG基础版
- [ ] 基本的聊天界面UI

**Week 2: 推荐系统**

- [ ] 实现资源推荐引擎
- [ ] 生成推荐理由（AI轻量生成）
- [ ] 澄清问题生成
- [ ] 资源卡片UI

**Week 3: 交互优化**

- [ ] 三阶段渐进式加载
- [ ] 骨架屏实现
- [ ] 基础动画和过渡
- [ ] 错误处理和降级

### Phase 2: 体验增强（1-2周）

**Week 4: 智能化升级**

- [ ] 智能约束放松（零结果处理）
- [ ] 迭代优化功能
- [ ] 短期会话记忆
- [ ] 短路逻辑（L1/L2切换）

**Week 5: 用户引导**

- [ ] 新用户引导（捷径诱惑）
- [ ] Mad Libs填空式引导
- [ ] Placeholder动效
- [ ] 示例问题轮换

### Phase 3: 移动端优化（1周）

**Week 6: 移动端特化**

- [ ] 全屏模式适配
- [ ] 手势交互（滑动、长按）
- [ ] 响应式布局调整
- [ ] 触摸优化（按钮尺寸、间距）

### Phase 4: 测试与迭代（1-2周）

**Week 7: 测试**

- [ ] 功能测试
- [ ] 性能测试
- [ ] 用户测试
- [ ] Bug修复

**Week 8: 上线准备**

- [ ] 代码优化和重构
- [ ] 文档完善
- [ ] 部署准备
- [ ] 监控和日志系统

### Phase 5: 持续优化（持续）

**监控指标**：

- 用户使用率
- 平均查询次数
- 结果满意度
- 响应时间
- API成本

**优化方向**：

- A/B测试引导方式
- 优化推荐算法
- 改进澄清策略
- 降低API成本

---

## 17. 成功指标（KPI）

### 17.1 用户指标

**使用率**：

- 日活用户 (DAU)
- 周活用户 (WAU)
- 月活用户 (MAU)
- 用户留存率

**使用深度**：

- 平均每会话查询次数
- 平均每用户查询次数/周
- 澄清问题回答率
- 迭代优化使用率

### 17.2 体验指标

**满意度**：

- 结果相关性评分（1-5分）
- 推荐理由帮助度（1-5分）
- NPS（净推荐值）
- 用户反馈情感分析

**性能**：

- 平均响应时间
- 首屏渲染时间
- 错误率
- API成本/查询

### 17.3 业务指标

**转化**：

- 资源详情页访问率
- 资源收藏率
- 资源分享率
- 用户回访率

**影响**：

- AI聊天 vs 传统搜索的对比
- 用户发现新资源的比例
- 用户满意度提升

---

## 18. 附录

### 18.1 术语表

| 术语                  | 定义                                         |
| --------------------- | -------------------------------------------- |
| RAG                   | Retrieval-Augmented Generation，检索增强生成 |
| CLS                   | Cumulative Layout Shift，累积布局偏移        |
| FCP                   | First Contentful Paint，首次内容绘制         |
| LCP                   | Largest Contentful Paint，最大内容绘制       |
| Mad Libs              | 填空式引导，源自填字游戏                     |
| Swimlanes             | 泳道，横向分组展示                           |
| Skeleton Screen       | 骨架屏，加载占位符                           |
| Constraint Relaxation | 约束放松，零结果处理策略                     |
| Short-circuit Logic   | 短路逻辑，快速路径优化                       |

### 18.2 参考资料

**产品设计**：

- [Google Material Design - Chatbots](https://material.io/design/communication/chat-bots.html)
- [NNGroup - UX for AI](https://www.nngroup.com/articles/ux-for-ai/)

**性能优化**：

- [Web Vitals](https://web.dev/vitals/)
- [Progressive Loading](https://web.dev/fast/#progressive-loading)

**AI/ML**：

- [LangChain Documentation](https://docs.langchain.com/)
- [Vercel AI SDK](https://sdk.vercel.ai/)

### 18.3 更新日志

**v1.0.0 (2026-01-22)**

- 初始版本
- 完成需求定义和技术规范
- 确定核心设计决策

---

**文档结束**

本文档定义了AI聊天助手的完整技术规范，包括核心定位、用户意图识别、澄清策略、资源呈现、响应优化、零结果处理、上下文记忆、移动端体验、用户引导、数据存储、AI边界、技术实现、UI/UX设计、错误处理、测试策略、实施路线图和成功指标。

所有核心设计决策已确定，可以进入开发阶段。
